name: validate
on:
  push:
  pull_request:
jobs:
  validate:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - name: Validate Nara source
        shell: pwsh
        run: .\build\Validate-Source.ps1
