# Packages

Only place versioned packages here after recording their source, SHA-256 hash, signature status, supported Windows builds, hardware requirements, verification, and rollback in `Catalog/package-catalog.json`.

No production package is present in this template.
