# Start Nara deployment

Analyze this fresh Windows installation using the local evidence in this workspace. Follow `AGENTS.md` exactly.

First validate the workspace and inventory. Then recommend the hardware profile, update mode, Nara runtime, and whether any local language model is appropriate. Resolve only compatible packages from the catalog and produce a dry-run deployment plan in `Reports/proposed-plan.json`.

Do not modify Windows yet. Summarize the recommendation, risks, expected idle impact, protected components, and the exact action IDs that would require my approval.
